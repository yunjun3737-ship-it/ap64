local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "파카허브|모든게임(일부제외)사용가능",
   LoadingTitle = "Paca Hub Comprehensive Loading...",
   LoadingSubtitle = "알파카 제작 입니다.",
   ConfigurationSaving = { Enabled = false },
   KeySystem = true,
   KeySettings = {
      Title = "파카허브 어드민 키시스템",
      Subtitle = "인증 키를 입력하세요",
      Note = "알파카제작 또한 일부 게임에선 이스크립트가 미작동 할수있다는걸 말씀드립니다.
키는 알파카입니다. 즐거운 사용바랍니다.",
      FileName = "PacaMegaKey",
      SaveKey = true,
      GrabKeyFromUrl = false,
      Key = {"알파카"}
   }
})

-- 탭 생성 (전부 한국어 변경)
local PlayerTab = Window:CreateTab("기본 플레이어", 4483362458)
local PhysicsTab = Window:CreateTab("물리 및 움직임", 4483362458)
local VisualTab = Window:CreateTab("시각 효과 설정", 4483362458)
local TeleportTab = Window:CreateTab("유저 텔레포트", 4483362458)
local ServerTab = Window:CreateTab("서버 및 환경", 4483362458)

-- 글로벌 변수 및 설정값 초기화
local _G = getgenv and getgenv() or _G
_G.WalkSpeed = 16
_G.JumpPower = 50
_G.HipHeight = 0
_G.Gravity = workspace.Gravity

local NoInertia = false
local FullBright = false
local SelectedPlayer = ""
local LoopTP = false
local CtrlClickTP = false
local AutoRespawn = false
local AntiSit = false
local NoClip = false

local Lighting = game:GetService("Lighting")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

-- 라이팅 원본 백업
local OrigAmbient = Lighting.Ambient
local OrigOutdoorAmbient = Lighting.OutdoorAmbient
local OrigBrightness = Lighting.Brightness
local OrigClockTime = Lighting.ClockTime
local OrigFogEnd = Lighting.FogEnd

local function GetPlayerNames()
   local names = {}
   for _, p in pairs(Players:GetPlayers()) do
      if p ~= LocalPlayer then table.insert(names, p.Name) end
   end
   return names
end

-------------------------------------------------------------------------------
-- 1. 플레이어 탭 (텍스트박스 입력 방식으로 고정 수리)
-------------------------------------------------------------------------------
PlayerTab:CreateInput({
   Name = "이동 속도 변경 (숫자 입력)",
   PlaceholderText = "기본값: 16",
   RemoveTextAfterFocusLost = false,
   Callback = function(Text)
      local num = tonumber(Text)
      if num then
         _G.WalkSpeed = num
         if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
            LocalPlayer.Character:FindFirstChildOfClass("Humanoid").WalkSpeed = num
         end
      end
   end
})

PlayerTab:CreateInput({
   Name = "점프력 변경 (숫자 입력)",
   PlaceholderText = "기본값: 50",
   RemoveTextAfterFocusLost = false,
   Callback = function(Text)
      local num = tonumber(Text)
      if num then
         _G.JumpPower = num
         if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
            local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
            hum.UseJumpPower = true
            hum.JumpPower = num
         end
      end
   end
})

PlayerTab:CreateInput({
   Name = "힙 높이 변경 (HipHeight)",
   PlaceholderText = "기본값: 0 (공중 부양 조절)",
   RemoveTextAfterFocusLost = false,
   Callback = function(Text)
      local num = tonumber(Text)
      if num then
         _G.HipHeight = num
         if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
            LocalPlayer.Character:FindFirstChildOfClass("Humanoid").HipHeight = num
         end
      end
   end
})

PlayerTab:CreateButton({
   Name = "캐릭터 강제 리셋 (Reset)",
   Callback = function()
      if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
         LocalPlayer.Character:FindFirstChildOfClass("Humanoid").Health = 0
      end
   end
})

PlayerTab:CreateToggle({
   Name = "자동 리스폰 활성화",
   CurrentValue = false,
   Flag = "AutoRespawnToggle",
   Callback = function(Value)
      AutoRespawn = Value
   end
})

-------------------------------------------------------------------------------
-- 2. 물리 및 움직임 탭
-------------------------------------------------------------------------------
PhysicsTab:CreateToggle({
   Name = "관성 제거 (즉시 제동)",
   CurrentValue = false,
   Flag = "NoInertiaToggle",
   Callback = function(Value)
      NoInertia = Value
   end
})

PhysicsTab:CreateToggle({
   Name = "의자 앉기 방지 (Anti-Sit)",
   CurrentValue = false,
   Flag = "AntiSitToggle",
   Callback = function(Value)
      AntiSit = Value
      if Value and LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
         LocalPlayer.Character:FindFirstChildOfClass("Humanoid").Sit = false
      end
   end
})

PhysicsTab:CreateToggle({
   Name = "벽 뚫기 (NoClip)",
   CurrentValue = false,
   Flag = "NoClipToggle",
   Callback = function(Value)
      NoClip = Value
   end
})

PhysicsTab:CreateInput({
   Name = "개인 중력 설정 (Gravity)",
   PlaceholderText = "기본값: 196.2",
   RemoveTextAfterFocusLost = false,
   Callback = function(Text)
      local num = tonumber(Text)
      if num then
         _G.Gravity = num
         workspace.Gravity = num
      end
   end
})

PhysicsTab:CreateButton({
   Name = "중력 상태 초기화",
   Callback = function()
      workspace.Gravity = 196.2
      _G.Gravity = 196.2
   end
})

-------------------------------------------------------------------------------
-- 3. 시각 효과 설정 탭
-------------------------------------------------------------------------------
VisualTab:CreateToggle({
   Name = "풀 브라이트 (야간 투시)",
   CurrentValue = false,
   Flag = "FullBrightToggle",
   Callback = function(Value)
      FullBright = Value
      if FullBright then
         Lighting.Ambient = Color3.fromRGB(255, 255, 255)
         Lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
         Lighting.Brightness = 2
         Lighting.ClockTime = 14
      else
         Lighting.Ambient = OrigAmbient
         Lighting.OutdoorAmbient = OrigOutdoorAmbient
         Lighting.Brightness = OrigBrightness
         Lighting.ClockTime = OrigClockTime
      end
   end
})

VisualTab:CreateToggle({
   Name = "맵 안개 제거 (Anti-Fog)",
   CurrentValue = false,
   Flag = "AntiFogToggle",
   Callback = function(Value)
      if Value then
         Lighting.FogEnd = 999999
      else
         Lighting.FogEnd = OrigFogEnd
      end
   end
})

VisualTab:CreateButton({
   Name = "그림자 제거",
   Callback = function()
      Lighting.GlobalShadows = false
   end
})

VisualTab:CreateButton({
   Name = "그림자 복구",
   Callback = function()
      Lighting.GlobalShadows = true
   end
})

-------------------------------------------------------------------------------
-- 4. 텔레포트 탭
-------------------------------------------------------------------------------
local PlayerDropdown = TeleportTab:CreateDropdown({
   Name = "대상 플레이어 선택",
   Options = GetPlayerNames(),
   CurrentOption = {""},
   MultipleOptions = false,
   Flag = "TargetDropdown",
   Callback = function(Option)
      SelectedPlayer = Option[1]
   end
})

TeleportTab:CreateButton({
   Name = "유저 목록 동기화 (새로고침)",
   Callback = function()
      PlayerDropdown:Refresh(GetPlayerNames(), true)
   end
})

TeleportTab:CreateButton({
   Name = "선택 유저에게 순간이동",
   Callback = function()
      if SelectedPlayer ~= "" and Players:FindFirstChild(SelectedPlayer) then
         local target = Players:FindFirstChild(SelectedPlayer)
         if target.Character and target.Character:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            LocalPlayer.Character.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame
         end
      end
   end
})

TeleportTab:CreateToggle({
   Name = "지속 추적 텔레포트 (Loop TP)",
   CurrentValue = false,
   Flag = "LoopTPToggle",
   Callback = function(Value)
      LoopTP = Value
   end
})

TeleportTab:CreateToggle({
   Name = "Ctrl + 클릭 위치로 텔레포트",
   CurrentValue = false,
   Flag = "CtrlClickToggle",
   Callback = function(Value)
      CtrlClickTP = Value
   end
})

-------------------------------------------------------------------------------
-- 5. 서버 및 환경 탭
-------------------------------------------------------------------------------
ServerTab:CreateButton({
   Name = "서버 재접속 (Rejoin)",
   Callback = function()
      game:GetService("TeleportService"):Teleport(game.PlaceId, LocalPlayer)
   end
})

ServerTab:CreateButton({
   Name = "가장 오래된 서버로 이동 (Server Hop)",
   Callback = function()
      pcall(function()
         local Http = game:GetService("HttpService")
         local Api = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"
         local List = Http:JSONDecode(game:HttpGet(Api))
         if List and List.data then
            for _, s in pairs(List.data) do
               if s.playing < s.maxPlayers and s.id ~= game.JobId then
                  game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, s.id, LocalPlayer)
                  break
               end
            end
         end
      end)
   end
})

-------------------------------------------------------------------------------
-- [루프 및 물리 상시 구동 프로세서]
-------------------------------------------------------------------------------
RunService.Heartbeat:Connect(function()
   pcall(function()
      if LocalPlayer.Character then
         local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
         local root = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
         
         -- 스피드 및 점프력 강제 고정 (안티 치트 프레임워크 우회 스태킹)
         if hum then
            if hum.WalkSpeed ~= _G.WalkSpeed then hum.WalkSpeed = _G.WalkSpeed end
            if hum.JumpPower ~= _G.JumpPower then hum.UseJumpPower = true; hum.JumpPower = _G.JumpPower end
            if hum.HipHeight ~= _G.HipHeight then hum.HipHeight = _G.HipHeight end
            if AntiSit and hum.Sit then hum.Sit = false end
         end
         
         -- 관성 제거 연산
         if NoInertia and root and hum and hum.MoveDirection.Magnitude == 0 then
            root.Velocity = Vector3.new(0, root.Velocity.Y, 0)
         end
         
         -- 벽 뚫기 가동
         if NoClip then
            for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
               if part:IsA("BasePart") then part.CanCollide = false end
            end
         end
      end
   end)
end)

-- 루프 텔레포트 프로세스
task.spawn(function()
   while task.wait(0.05) do
      if LoopTP and SelectedPlayer ~= "" and Players:FindFirstChild(SelectedPlayer) then
         pcall(function()
            local target = Players:FindFirstChild(SelectedPlayer)
            if target.Character and target.Character:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
               LocalPlayer.Character.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame
            end
         end)
      end
   end
end)

-- 시각 밝기 루프 홀딩
task.spawn(function()
   while task.wait(0.5) do
      if FullBright then
         Lighting.Ambient = Color3.fromRGB(255, 255, 255)
         Lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
         Lighting.Brightness = 2
         Lighting.ClockTime = 14
      end
   end
end)

-- 자동 리스폰 감지
LocalPlayer.CharacterRemoving:Connect(function()
   if AutoRespawn then
      task.wait(0.1)
      LocalPlayer:LoadCharacter()
   end
end)

-- Ctrl + 클릭 텔레포트 기능 구현
UserInputService.InputBegan:Connect(function(input, processed)
   if not processed and CtrlClickTP and input.UserInputType == Enum.UserInputType.MouseButton1 and UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
      local mouse = LocalPlayer:GetMouse()
      if mouse.Target and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
         LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(mouse.Hit.Position + Vector3.new(0, 3, 0))
      end
   end
end)

-- 자동 동기화 리스너 등록
Players.PlayerAdded:Connect(function() PlayerDropdown:Refresh(GetPlayerNames(), true) end)
Players.PlayerRemoving:Connect(function() PlayerDropdown:Refresh(GetPlayerNames(), true) end)
-------------------------------------------------------------------------------
-- 6. 어드민 및 특수 기능 탭
-------------------------------------------------------------------------------
local AdminTab = Window:CreateTab("어드민 기능", 4483362458)

local Fly = false
local FlySpeed = 50
local InfiniteJump = false
local ESPEnabled = false
local AntiAFK = false
local SpectateTarget = ""
local SavedPosition = nil
local CurrentSpectate = nil

-------------------------------------------------------------------------------
-- 플라이 기능
-------------------------------------------------------------------------------
local BV
local BG

local function StartFly()
    local char = LocalPlayer.Character
    if not char then return end

    local root = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not root or not hum then return end

    BG = Instance.new("BodyGyro")
    BG.P = 9e4
    BG.Parent = root
    BG.MaxTorque = Vector3.new(9e9,9e9,9e9)
    BG.CFrame = root.CFrame

    BV = Instance.new("BodyVelocity")
    BV.Parent = root
    BV.MaxForce = Vector3.new(9e9,9e9,9e9)
    BV.Velocity = Vector3.zero

    hum.PlatformStand = true

    task.spawn(function()
        while Fly and task.wait() do
            local cam = workspace.CurrentCamera
            local moveDir = Vector3.zero

            if UserInputService:IsKeyDown(Enum.KeyCode.W) then
                moveDir += cam.CFrame.LookVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then
                moveDir -= cam.CFrame.LookVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then
                moveDir -= cam.CFrame.RightVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then
                moveDir += cam.CFrame.RightVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
                moveDir += Vector3.new(0,1,0)
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
                moveDir -= Vector3.new(0,1,0)
            end

            BV.Velocity = moveDir * FlySpeed
            BG.CFrame = cam.CFrame
        end
    end)
end

local function StopFly()
    local char = LocalPlayer.Character
    if char and char:FindFirstChildOfClass("Humanoid") then
        char:FindFirstChildOfClass("Humanoid").PlatformStand = false
    end

    if BV then BV:Destroy() end
    if BG then BG:Destroy() end
end

AdminTab:CreateToggle({
    Name = "플라이 (Fly)",
    CurrentValue = false,
    Flag = "FlyToggle",
    Callback = function(Value)
        Fly = Value

        if Value then
            StartFly()
        else
            StopFly()
        end
    end
})

AdminTab:CreateInput({
    Name = "플라이 속도",
    PlaceholderText = "기본값: 50",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local num = tonumber(Text)
        if num then
            FlySpeed = num
        end
    end
})

-------------------------------------------------------------------------------
-- 무한 점프
-------------------------------------------------------------------------------
AdminTab:CreateToggle({
    Name = "무한 점프",
    CurrentValue = false,
    Flag = "InfiniteJumpToggle",
    Callback = function(Value)
        InfiniteJump = Value
    end
})

UserInputService.JumpRequest:Connect(function()
    if InfiniteJump and LocalPlayer.Character then
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            hum:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end
end)

-------------------------------------------------------------------------------
-- ESP 기능
-------------------------------------------------------------------------------
local ESPTable = {}

local function CreateESP(player)
    if player == LocalPlayer then return end

    local highlight = Instance.new("Highlight")
    highlight.FillTransparency = 0.5
    highlight.OutlineTransparency = 0
    highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    highlight.Parent = game.CoreGui

    ESPTable[player] = highlight

    local function Update()
        if player.Character then
            highlight.Adornee = player.Character
        end
    end

    Update()
    player.CharacterAdded:Connect(Update)
end

local function RemoveESP()
    for _, v in pairs(ESPTable) do
        pcall(function()
            v:Destroy()
        end)
    end
    ESPTable = {}
end

AdminTab:CreateToggle({
    Name = "플레이어 ESP",
    CurrentValue = false,
    Flag = "ESPToggle",
    Callback = function(Value)
        ESPEnabled = Value

        if Value then
            for _, p in pairs(Players:GetPlayers()) do
                CreateESP(p)
            end
        else
            RemoveESP()
        end
    end
})

Players.PlayerAdded:Connect(function(p)
    if ESPEnabled then
        CreateESP(p)
    end
end)

-------------------------------------------------------------------------------
-- 관전 기능
-------------------------------------------------------------------------------
local SpectateDropdown = AdminTab:CreateDropdown({
    Name = "관전 대상 선택",
    Options = GetPlayerNames(),
    CurrentOption = {""},
    MultipleOptions = false,
    Flag = "SpectateDropdown",
    Callback = function(Option)
        SpectateTarget = Option[1]
    end
})

AdminTab:CreateButton({
    Name = "관전 시작",
    Callback = function()
        if Players:FindFirstChild(SpectateTarget) then
            local target = Players:FindFirstChild(SpectateTarget)

            if target.Character and target.Character:FindFirstChildOfClass("Humanoid") then
                workspace.CurrentCamera.CameraSubject = target.Character:FindFirstChildOfClass("Humanoid")
                CurrentSpectate = target
            end
        end
    end
})

AdminTab:CreateButton({
    Name = "관전 종료",
    Callback = function()
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
            workspace.CurrentCamera.CameraSubject = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        end
    end
})

-------------------------------------------------------------------------------
-- FOV 변경
-------------------------------------------------------------------------------
AdminTab:CreateInput({
    Name = "시야각 변경 (FOV)",
    PlaceholderText = "기본값: 70",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local num = tonumber(Text)
        if num then
            workspace.CurrentCamera.FieldOfView = num
        end
    end
})

-------------------------------------------------------------------------------
-- 좌표 저장 / 복귀
-------------------------------------------------------------------------------
AdminTab:CreateButton({
    Name = "현재 위치 저장",
    Callback = function()
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            SavedPosition = LocalPlayer.Character.HumanoidRootPart.CFrame
        end
    end
})

AdminTab:CreateButton({
    Name = "저장 위치로 이동",
    Callback = function()
        if SavedPosition and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            LocalPlayer.Character.HumanoidRootPart.CFrame = SavedPosition
        end
    end
})

-------------------------------------------------------------------------------
-- FPS 부스터
-------------------------------------------------------------------------------
AdminTab:CreateButton({
    Name = "FPS 부스터",
    Callback = function()
        for _, v in pairs(workspace:GetDescendants()) do
            pcall(function()
                if v:IsA("BasePart") then
                    v.Material = Enum.Material.Plastic
                    v.Reflectance = 0
                elseif v:IsA("Decal") or v:IsA("Texture") then
                    v.Transparency = 1
                elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                    v.Enabled = false
                end
            end)
        end

        Lighting.GlobalShadows = false
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
    end
})

-------------------------------------------------------------------------------
-- Anti AFK
-------------------------------------------------------------------------------
AdminTab:CreateToggle({
    Name = "Anti AFK",
    CurrentValue = false,
    Flag = "AntiAFKToggle",
    Callback = function(Value)
        AntiAFK = Value

        if Value then
            local vu = game:GetService("VirtualUser")

            LocalPlayer.Idled:Connect(function()
                if AntiAFK then
                    vu:CaptureController()
                    vu:ClickButton2(Vector2.new())
                end
            end)
        end
    end
})

-------------------------------------------------------------------------------
-- 리프레시
-------------------------------------------------------------------------------
AdminTab:CreateButton({
    Name = "플레이어 목록 새로고침",
    Callback = function()
        SpectateDropdown:Refresh(GetPlayerNames(), true)
    end
})
-------------------------------------------------------------------------------
-- 카메라 탭
-------------------------------------------------------------------------------
local CameraTab = Window:CreateTab("카메라", 4483362458)

local ThirdPerson = false
local ThirdPersonDistance = 25

CameraTab:CreateToggle({
    Name = "3인칭 모드",
    CurrentValue = false,
    Flag = "ThirdPersonToggle",
    Callback = function(Value)
        ThirdPerson = Value

        if Value then
            LocalPlayer.CameraMode = Enum.CameraMode.Classic
            LocalPlayer.CameraMinZoomDistance = ThirdPersonDistance
            LocalPlayer.CameraMaxZoomDistance = 999999
        else
            LocalPlayer.CameraMode = Enum.CameraMode.LockFirstPerson
            LocalPlayer.CameraMinZoomDistance = 0
            LocalPlayer.CameraMaxZoomDistance = 0
        end
    end
})

CameraTab:CreateInput({
    Name = "3인칭 거리",
    PlaceholderText = "기본값: 25",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local num = tonumber(Text)

        if num then
            ThirdPersonDistance = num

            if ThirdPerson then
                LocalPlayer.CameraMinZoomDistance = num
            end
        end
    end
})

-------------------------------------------------------------------------------
-- 추가 기능 탭
-------------------------------------------------------------------------------
local ExtraTab = Window:CreateTab("추가 기능", 4483362458)

local SpinEnabled = false
local SpinSpeed = 5
local AntiVoid = false
local AutoJump = false
local RainbowCharacter = false
local InvisibleCharacter = false
local FreezeCharacter = false
local AntiFling = false
local AutoRotate = false
local TinyCharacter = false
local GiantCharacter = false
local WaterWalk = false
local WalkOnAir = false
local DiscoMode = false
local HeadSpin = false
local RainbowLighting = false
local OrbitSpin = false
local AutoSit = false
local AutoDance = false
local CameraShake = false
local FlashMode = false
local BlurMode = false
local LagVisual = false
local FakeAFK = false
local AntiRagdoll = false
local ReverseGravity = false
local RandomJump = false
local NeonMode = false
local InvisibleName = false
local ShadowSpam = false
local GravitySpam = false
local RandomBrightness = false
local ClockFreeze = false
local AutoRotateCamera = false
local SpinBot = false
local FastSpin = false
local JumpSpin = false
local PlatformMode = false
local FakeLag = false
local AutoHeal = false
local AutoReset = false
local FakeFreeze = false
local MoonGravity = false
local ZeroGravity = false
local FastAnimation = false
local SlowAnimation = false
local RainbowBody = false
local AutoZoom = false
local AutoFlash = false
local SkyFlash = false
local AntiFall = false
local InfiniteZoom = false
local MiniMode = false

ExtraTab:CreateToggle({
    Name = "회전 스핀",
    CurrentValue = false,
    Flag = "SpinToggle",
    Callback = function(Value)
        SpinEnabled = Value
    end
})

ExtraTab:CreateInput({
    Name = "스핀 속도",
    PlaceholderText = "기본값: 5",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local num = tonumber(Text)

        if num then
            SpinSpeed = num
        end
    end
})

ExtraTab:CreateToggle({
    Name = "낙사 방지",
    CurrentValue = false,
    Flag = "AntiVoidToggle",
    Callback = function(Value)
        AntiVoid = Value
    end
})

ExtraTab:CreateToggle({
    Name = "자동 점프",
    CurrentValue = false,
    Flag = "AutoJumpToggle",
    Callback = function(Value)
        AutoJump = Value
    end
})

ExtraTab:CreateToggle({
    Name = "무지개 캐릭터",
    CurrentValue = false,
    Flag = "RainbowCharacterToggle",
    Callback = function(Value)
        RainbowCharacter = Value
    end
})

ExtraTab:CreateToggle({
    Name = "캐릭터 투명화",
    CurrentValue = false,
    Flag = "InvisibleCharacterToggle",
    Callback = function(Value)
        InvisibleCharacter = Value

        if LocalPlayer.Character then
            for _, v in pairs(LocalPlayer.Character:GetDescendants()) do
                if v:IsA("BasePart") then
                    v.Transparency = Value and 0.6 or 0
                end
            end
        end
    end
})

ExtraTab:CreateToggle({
    Name = "캐릭터 고정",
    CurrentValue = false,
    Flag = "FreezeCharacterToggle",
    Callback = function(Value)
        FreezeCharacter = Value
    end
})

ExtraTab:CreateToggle({
    Name = "안티 플링",
    CurrentValue = false,
    Flag = "AntiFlingToggle",
    Callback = function(Value)
        AntiFling = Value
    end
})

ExtraTab:CreateToggle({
    Name = "자동 회전",
    CurrentValue = false,
    Flag = "AutoRotateToggle",
    Callback = function(Value)
        AutoRotate = Value
    end
})

ExtraTab:CreateToggle({
    Name = "작은 캐릭터",
    CurrentValue = false,
    Flag = "TinyCharacterToggle",
    Callback = function(Value)
        TinyCharacter = Value
    end
})

ExtraTab:CreateToggle({
    Name = "거대 캐릭터",
    CurrentValue = false,
    Flag = "GiantCharacterToggle",
    Callback = function(Value)
        GiantCharacter = Value
    end
})

ExtraTab:CreateToggle({
    Name = "물 위 걷기",
    CurrentValue = false,
    Flag = "WaterWalkToggle",
    Callback = function(Value)
        WaterWalk = Value
    end
})

ExtraTab:CreateToggle({
    Name = "공중 걷기",
    CurrentValue = false,
    Flag = "WalkOnAirToggle",
    Callback = function(Value)
        WalkOnAir = Value
    end
})

ExtraTab:CreateToggle({
    Name = "디스코 모드",
    CurrentValue = false,
    Flag = "DiscoModeToggle",
    Callback = function(Value)
        DiscoMode = Value
    end
})

ExtraTab:CreateToggle({
    Name = "머리 회전",
    CurrentValue = false,
    Flag = "HeadSpinToggle",
    Callback = function(Value)
        HeadSpin = Value
    end
})

ExtraTab:CreateToggle({
    Name = "무지개 조명",
    CurrentValue = false,
    Flag = "RainbowLightingToggle",
    Callback = function(Value)
        RainbowLighting = Value
    end
})

ExtraTab:CreateToggle({
    Name = "궤도 회전",
    CurrentValue = false,
    Flag = "OrbitSpinToggle",
    Callback = function(Value)
        OrbitSpin = Value
    end
})

ExtraTab:CreateToggle({
    Name = "자동 앉기",
    CurrentValue = false,
    Flag = "AutoSitToggle",
    Callback = function(Value)
        AutoSit = Value
    end
})

ExtraTab:CreateToggle({
    Name = "자동 춤",
    CurrentValue = false,
    Flag = "AutoDanceToggle",
    Callback = function(Value)
        AutoDance = Value
    end
})

ExtraTab:CreateToggle({
    Name = "카메라 흔들림",
    CurrentValue = false,
    Flag = "CameraShakeToggle",
    Callback = function(Value)
        CameraShake = Value
    end
})

ExtraTab:CreateToggle({
    Name = "플래시 모드",
    CurrentValue = false,
    Flag = "FlashModeToggle",
    Callback = function(Value)
        FlashMode = Value
    end
})

ExtraTab:CreateToggle({
    Name = "블러 모드",
    CurrentValue = false,
    Flag = "BlurModeToggle",
    Callback = function(Value)
        BlurMode = Value
    end
})

ExtraTab:CreateToggle({
    Name = "가짜 렉",
    CurrentValue = false,
    Flag = "LagVisualToggle",
    Callback = function(Value)
        LagVisual = Value
    end
})

ExtraTab:CreateToggle({
    Name = "가짜 잠수",
    CurrentValue = false,
    Flag = "FakeAFKToggle",
    Callback = function(Value)
        FakeAFK = Value
    end
})

ExtraTab:CreateToggle({
    Name = "안티 래그돌",
    CurrentValue = false,
    Flag = "AntiRagdollToggle",
    Callback = function(Value)
        AntiRagdoll = Value
    end
})

ExtraTab:CreateToggle({
    Name = "역중력",
    CurrentValue = false,
    Flag = "ReverseGravityToggle",
    Callback = function(Value)
        ReverseGravity = Value
    end
})

ExtraTab:CreateToggle({
    Name = "랜덤 점프",
    CurrentValue = false,
    Flag = "RandomJumpToggle",
    Callback = function(Value)
        RandomJump = Value
    end
})

ExtraTab:CreateToggle({
    Name = "네온 모드",
    CurrentValue = false,
    Flag = "NeonModeToggle",
    Callback = function(Value)
        NeonMode = Value
    end
})

ExtraTab:CreateToggle({
    Name = "닉네임 숨기기",
    CurrentValue = false,
    Flag = "InvisibleNameToggle",
    Callback = function(Value)
        InvisibleName = Value
    end
})

ExtraTab:CreateToggle({
    Name = "그림자 스팸",
    CurrentValue = false,
    Flag = "ShadowSpamToggle",
    Callback = function(Value)
        ShadowSpam = Value
    end
})

ExtraTab:CreateToggle({
    Name = "중력 스팸",
    CurrentValue = false,
    Flag = "GravitySpamToggle",
    Callback = function(Value)
        GravitySpam = Value
    end
})

ExtraTab:CreateToggle({
    Name = "랜덤 밝기",
    CurrentValue = false,
    Flag = "RandomBrightnessToggle",
    Callback = function(Value)
        RandomBrightness = Value
    end
})

ExtraTab:CreateToggle({
    Name = "시간 정지",
    CurrentValue = false,
    Flag = "ClockFreezeToggle",
    Callback = function(Value)
        ClockFreeze = Value
    end
})

ExtraTab:CreateToggle({
    Name = "자동 카메라 회전",
    CurrentValue = false,
    Flag = "AutoRotateCameraToggle",
    Callback = function(Value)
        AutoRotateCamera = Value
    end
})

ExtraTab:CreateToggle({
    Name = "스핀봇",
    CurrentValue = false,
    Flag = "SpinBotToggle",
    Callback = function(Value)
        SpinBot = Value
    end
})

ExtraTab:CreateToggle({
    Name = "고속 스핀",
    CurrentValue = false,
    Flag = "FastSpinToggle",
    Callback = function(Value)
        FastSpin = Value
    end
})

ExtraTab:CreateToggle({
    Name = "점프 스핀",
    CurrentValue = false,
    Flag = "JumpSpinToggle",
    Callback = function(Value)
        JumpSpin = Value
    end
})

ExtraTab:CreateToggle({
    Name = "플랫폼 모드",
    CurrentValue = false,
    Flag = "PlatformModeToggle",
    Callback = function(Value)
        PlatformMode = Value
    end
})

ExtraTab:CreateToggle({
    Name = "가짜 프리즈",
    CurrentValue = false,
    Flag = "FakeFreezeToggle",
    Callback = function(Value)
        FakeFreeze = Value
    end
})

ExtraTab:CreateToggle({
    Name = "달 중력",
    CurrentValue = false,
    Flag = "MoonGravityToggle",
    Callback = function(Value)
        MoonGravity = Value
    end
})

ExtraTab:CreateToggle({
    Name = "무중력",
    CurrentValue = false,
    Flag = "ZeroGravityToggle",
    Callback = function(Value)
        ZeroGravity = Value
    end
})

ExtraTab:CreateToggle({
    Name = "빠른 애니메이션",
    CurrentValue = false,
    Flag = "FastAnimationToggle",
    Callback = function(Value)
        FastAnimation = Value
    end
})

ExtraTab:CreateToggle({
    Name = "느린 애니메이션",
    CurrentValue = false,
    Flag = "SlowAnimationToggle",
    Callback = function(Value)
        SlowAnimation = Value
    end
})

ExtraTab:CreateToggle({
    Name = "무지개 몸체",
    CurrentValue = false,
    Flag = "RainbowBodyToggle",
    Callback = function(Value)
        RainbowBody = Value
    end
})

ExtraTab:CreateToggle({
    Name = "자동 줌",
    CurrentValue = false,
    Flag = "AutoZoomToggle",
    Callback = function(Value)
        AutoZoom = Value
    end
})

ExtraTab:CreateToggle({
    Name = "자동 플래시",
    CurrentValue = false,
    Flag = "AutoFlashToggle",
    Callback = function(Value)
        AutoFlash = Value
    end
})

ExtraTab:CreateToggle({
    Name = "하늘 번쩍임",
    CurrentValue = false,
    Flag = "SkyFlashToggle",
    Callback = function(Value)
        SkyFlash = Value
    end
})

ExtraTab:CreateToggle({
    Name = "낙하 방지",
    CurrentValue = false,
    Flag = "AntiFallToggle",
    Callback = function(Value)
        AntiFall = Value
    end
})

ExtraTab:CreateToggle({
    Name = "무한 줌",
    CurrentValue = false,
    Flag = "InfiniteZoomToggle",
    Callback = function(Value)
        InfiniteZoom = Value
    end
})

ExtraTab:CreateToggle({
    Name = "미니 모드",
    CurrentValue = false,
    Flag = "MiniModeToggle",
    Callback = function(Value)
        MiniMode = Value
    end
})
